require 'rails_helper'
RSpec.describe "categories#index", :type => :view do
  context "on the index page" do
    describe "the page" do
      xit "should have a list of all categories available" do
        expect(true).to eq(true)
      end
    end
  end
end
